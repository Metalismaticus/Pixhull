# Текущая партия

Файл ведёт чат разработки: `/start` записывает сюда снятый список, `/done`
очищает. Чат замысла его не правит — так два чата в одной папке не пишут в
один файл одновременно (`CLAUDE.md`, «Два чата»).

Снята `/start all` 2026-09-24 с «Очереди» `docs/ROADMAP.md` на `b58792f`.
Данные пользователя: не задевает (`docs/TESTING.md`, «Данные пользователя»).
Исходная проверка: полная, зелёная на `b58792f` — `check.mjs` 39 модулей, `mcp-smoke.mjs` все,
полигон 28 зелёных / 0 провалов. Полигон — «есть».

**Условие владельца, действует и здесь:** «главное не вредить продукту, если наша
картинка образец источник проблем». Дефект продукта (воспроизводится на синтетике
из `tests/fixtures.mjs`) — чинить; свойство грязного входа — назвать числом и не подгонять
пороги. `tests/art/truck.png` теперь в репозитории (`6c40d2c`) — замеры по нему
воспроизводимы, но полигон его по-прежнему не читает намеренно.

| № | Пункт очереди | Критерий готовности | Состояние | В документы при `/done` | Свои числа |
|---|---|---|---|---|---|
| 1 | **[баг]** Ложное срабатывание «одинаковых видов» на демо (очередь 1) | «Демо» собирается без предупреждения, а синтетическая пара «один и тот же рисунок в двух слотах» по-прежнему ловится — **обе ситуации проверками в полигоне**. «Нельзя ломать» из `BUGS.md`: просто поднять порог формы до 0.9 нельзя — проверка заведена против настоящего случая (виды в три четверти) | готов к проверке (2 круга) | ROADMAP «Сделано» — демо больше не встречает предупреждением; BUGS — удалить запись «Демо встречает предупреждением об ошибке»; TESTING — **новая группа `views`** (`views/lookalike-demo`, `views/lookalike-duplicate`), полигон 28 → 30; CONCEPT — `src/demo.js` больше не требует canvas (рисунки в RGBA8), поэтому демо проверяется из Node; **README — строки 204–210 теперь противоречат поведению**: там написано «aspect and placement cannot disguise the match», а аспект теперь как раз и отсеивает пару; **BUGS/очередь — записать оставшийся риск:** смешанная пара «три четверти против ортографии» теперь отбрасывается молча | Порог `PROPORTION_TOLERANCE = 1.15` **выбран, а не измерен**: ворота сохраняют пару, только если **оба** рисунка — ракурсы в три четверти (отношение 1.000–1.011, независимый прогон reviewer дал 1.000–1.021). **3/4 в одном слоте против ортографического — расходятся в 1.2–1.9, и диагноз молчит.** Замер на `tests/art/truck.png` (trim): front 308×349 = 0.883, right 505×291 = 1.735, top 479×244 = 1.963, 3/4 при рыскании 45° ≈ 1.647; отношение к front **1.87**, к top **1.19** — оба выше 1.15. Снизу порог полигоном **не стережётся**: все три случая `lookalike-duplicate` дают 1.000, затяжка до 1.001 оставляет 30/0; сверху краснеет `lookalike-demo` выше 1.92. Пары демо: 1.917 / 2.200 / 4.217; демо карвит 1438 вокселей, 0 предупреждений. Порог формы не тронут (запрет «Нельзя ломать» соблюдён). **Ограничение reviewer:** настоящего листа с ракурсами в три четверти в репозитории нет — «Нельзя ломать» эмпирически непроверяемо, судили по реконструкции |
| 2 | **[ui]** Бокс становится выделением, и при правке видна сетка (очередь 2) | Сначала `designer` пишет спецификацию. У бокса появляется третье измерение в самом жесте, а не из ползунка кисти; «границы сетки» дают решётку по клеткам, по которой можно целиться. Владелец просил дважды — это его ежедневная работа. Полный критерий — `ROADMAP.md`, «Подробности» | ждёт очереди | | |
| 3 | **[ui]** Карта расхождений: показать, где рисунки спорят (очередь 3) | Сначала `designer`. Решение владельца 2026-09-24: расхождения арта **не чиним, а показываем**. У грузовика есть что показывать, числа уже замерены и лежат ниже в «Найдено по ходу»: просвет у зеркал (603 пикселя, front уже top на 2 клетки на 22 строках) и расхождение по высоте кабины (медиана 9 вокселей на 256, 59 на 512). Полный критерий — `ROADMAP.md`, «Подробности» | ждёт очереди | | |
| 4 | **[баг]** Скаты красятся не тем видом: белая крыша, синяя полоска, чересполосица (очередь 4) | **ВНИМАНИЕ — в `BUGS.md` причина названа НЕВЕРНО, поправка ещё не перенесена чатом замысла.** Запись говорит `repaintSlopes`, а замер (см. «Найдено по ходу» ниже) дал: белых граней крыши 557 на 256 и 3206 на 512, из них **487 и 2421 ставит `paintFromViews`**, а не `repaintSlopes`. Правило ничьей виновато только в 10–11% (56 и 361). **Предложение «отдать ничью виду сверху» уже отклонено числом** (лечит 42, портит 1481 — 35:1) — не повторять этот ход. Снимать проверку глубины в `repaintSlopes` тоже нельзя — `carve/reprojection` краснеет 8/10112. **И ещё: полигон правило ничьей не стережёт вообще** (с вывернутым правилом прогон 28/0) — нужна проверка, иначе починка ничем не защищена | ждёт очереди | | |

Состояния: ждёт очереди · в работе · готов к проверке · ждёт: <вопрос> ·
не выполнен: <замечания>

<!-- Образец шапки и таблицы, которые пишет `/start`:

Снята `/start all` <дата> с «Очереди» `docs/ROADMAP.md` на <коммит>.
Данные пользователя: <задевает или нет, со ссылкой на docs/TESTING.md>.
Исходная проверка: <уровень>, <вердикт> на <коммит>.

| № | Пункт очереди | Критерий готовности | Состояние | В документы при `/done` | Свои числа |
|---|---|---|---|---|---|
| 1 | **[метка]** <пункт> (очередь N) | <критерий> | ждёт очереди | <что внести и куда> | <пороги и замеры> |

Состояния: ждёт очереди · в работе · готов к проверке · ждёт: <вопрос> ·
не выполнен: <замечания>

Номера в этой таблице — свои, сквозные; в скобках указан номер пункта в
«Очереди» `docs/ROADMAP.md` на момент снимка. `/done кроме 3` считает по
левой колонке.
-->

## Пришло — внести в очередь

Сюда пишет `/add`; переносит в `ROADMAP.md` и `BLOCKED.md` чат замысла.

## Найдено по ходу — внести в очередь

Сюда пишет координатор `/start`: что исполнители заметили вне своего пункта.

- **Положения ползунка «Скругление» 5 и 6 не делают ничего.** `Math.min(4, relax)` в `src/export/surfacenets.js` делает меши при 4, 5 и 6 побайтово одинаковыми (проверено reviewer), а в `index.html` у ползунка семь делений — работают пять. Замечено в пункте 1.
- **Полигон и `mcp-smoke.mjs` не смотрят экспорт `.obj`/`.glb` на замкнутость.** `export/smooth-mesh` мерит меш, но не то, что писатели переносят его дословно. Замечено в пункте 1.
- **`medianCut` строит и возвращает `assign`, который больше никто не читает** (`src/core/quantize.js`) — лишняя Map на все цвета. Мёртвая работа, не дефект. Замечено в пункте 2.

### Найдено разбором жалоб владельца по второй партии

- **Запись `BUGS.md` «Крыша кабины красится белым» НЕ СТОИТ В ОЧЕРЕДИ.** `BUGS.md` хранит подробности, но место задаёт очередь — без строки в `ROADMAP.md` этот баг `/start` не возьмёт никогда.
- **Там же неверно названа причина.** Запись говорит  `repaintSlopes`, а замер: 487 из 557 граней на 256 и 2421 из 3206 на 512 ставит **`paintFromViews`** (`src/core/carve.js`). Следующий исполнитель починит не тот код.
- **Предложение «отдать ничью виду сверху» отклонено числом:** лечит 42 грани, портит заметно 1481 (256) — 35:1; на 512 — 361 против 4955, 13.7:1. Теряются стекло, решётка, фонари.
- **ДЫРА ПОЛИГОНА: правило ничьей не стережётся вообще.** С правилом, вывернутым наизнанку, полный прогон — 28 зелёных / 0, а `carve/slope-banding` даже «улучшается» 84 → 8. Потерю 1481 грани проверки не видят: синтетика одноцветна по виду. Нужна проверка.
- **Вырезы на боках кузова — два жёлоба глубиной ровно 2 вокселя** во всю длину фургона: верхний 166×22 клетки (3652), нижний 166×8 (1292). Справа утоплено 5110 клеток (25.5%), слева 4617 (23.1%). **Причина:** передний рисунок в полосе зеркал идёт до x=178, а верхний и задний говорят до x=180 — противоречие в 2 клетки на 22 строках, и пересечение размазывает его по всей длине. Это 603 прозрачных пикселя из 308×349: в рендере между кабиной и зеркалом просвечивает фон. Заливка этих 603 пикселей → +19 886 вокселей, жёлоб исчезает. **Давнее, не регресс:** на `dece678` числа совпадают до единицы. Гладкий меш не углубляет (2.000 против 1.999).
- **Продуктовая дыра за этим вырезом (кандидат в очередь):** `clipContestedSpikes` (`src/core/views.js`) мирит **выступы**, которые один рисунок показывает, а другой нет, и **не имеет двойника для выемок**. Выемка, которую один вид показывает, а вид по той же оси закрывает сплошняком, размазывается по всей третьей оси. **Это новая функция, меняющая геометрию, а не починка бага** — решение владельца.
- **Дешёвая честная замена (кандидат в очередь):** диагностика «вид X уже вида Y на N клеток на M строках». Здесь она сказала бы «front уже top на 2 клетки на 22 строках» и сразу назвала виноватый рисунок.
- Серые полосы на крыше кузова — **из арта**: 11 из 11 на 256 и 21 из 21 на 512 совпадают с видом сверху. Продукт не добавляет ни одной.
- `bottom` шире модели на 14–15 клеток с каждой стороны (отсюда его 5138 «не добралось»), но он шире, а не уже, и материала не снимает.

- **Рисунок с чётностью ширины, противоположной сетке, теряет целый столбец модели.** Замер `tests/edit/mirror-parity.mjs` (пункт 3): 8 случаев из 16 — нарисовано 9 px, вырезано 8 (сетка 32); нарисовано 8, вырезано 7 (сетка 33). Причина: `autoPlace` округляет половину зазора одинаково для всех видов (`src/core/views.js`), но у вида `back` ось u идёт обратно (`N - 1 - u`, `src/core/views.js:34`), поэтому перёд и зад садятся на клетку врозь и их пересечение уже любого из них. Дефект карвинга, не редактора.
- **При расхождении переднего и заднего рисунков по ширине на нечётное число пикселей модель стоит на полвокселя мимо оси сетки**, и зеркальная правка ложится соседом (замерено: смещение −1 при 9 и 12 px). Тот же замер. Тоже карвинг.
- Исходная гипотеза пункта 3 («`autoPlace` и `mirrorX` считают центр по-разному») **замером опровергнута** — 16/16, снос 0. В очередь ставить нечего, но в `BUGS.md` этого предположения больше быть не должно.
- **Сохранённый и снова загруженный проект теряет таблицу соответствия цветов** (`serialize` хранит только сами цвета). На модель не влияет, влияет на добор цвета пипеткой после загрузки. Замечено в пункте 4.
- **`src/main.js`, обработчик `keydown` делает `e.target.matches(...)` без проверки, что цель — элемент.** При событии на `window`/`document` бросает `TypeError`. Настоящей клавиатурой недостижимо, но ломает любой синтетический замер горячих клавиш — то есть мешает полигону. Замечено в пункте 5.
- **«Очистить» при пустых слотах не сбрасывает палитру**: `build()` выходит раньше, и в палитре остаются цвета снесённой модели. Замечено в пункте 6.
- **`src/gfx/renderer.js` — цвет контура превью был зашит числами** (акцент тёмной темы), в светлой неверный — нарушение `DESIGN.md` §2. Нашёл `designer`, исправлено в пункте 6 (читается из `--accent`). Запись здесь — чтобы не потерять правило.
- **Обработчик `drop` на `document` зовёт `e.target.closest`** — при событии на сам `document` бросает `TypeError`. Та же болезнь, что у `keydown` из пункта 5. Замечено в пункте 8.
- **Кнопки «Загрузить проект», «Демо» и загрузка вида во время сборки не выключены.** Данные больше не теряются (пункт 8), но панель молчит о том, что сборка снята. Замечено в пункте 8.
- **`undo()`/`redo()` во время сборки говорят `status.nothingToUndo`** — формально неправда: история есть, просто занята. `DESIGN.md` требует объяснять отказ. Замечено в пункте 8.
- **`mcp-smoke.mjs` не смотрит содержимое `sprites.json` дальше размеров листа** — правку палитры в MCP он бы не поймал. Полигонной проверки на экспорт MCP нет. Замечено в пункте 9.
- **`css/app.css` — `.hint.warn { color: var(--warn, #d08a3a) }` держит зашитый запасной цвет** вопреки `DESIGN.md` §2 (цвет через переменную). Замечено в пункте 9.
- **После слияния повторное добавление цвета из арта возвращает прежний слот-псевдоним**, а не свободный: псевдонимы квантизации намеренно переживают слияние, свободные слоты достаются только новым цветам пользователя. Замечено в пункте 9.
- **Дефект самой проверки `palette/accuracy`:** она печатает долю точных совпадений и число сохранённых цветов, но **ни то ни другое не утверждает**: обвал 81.8% → 24.2% и 255 → 90 цветов она пропускает зелёной — утверждения стоят только на ΔE. Нужен нижний порог на оба числа (замер 2026-09-24: 81.8% и 255 из 324 при исправлении, 24.2% и 90 без цикла возврата слотов). Дефект проверки, не продукта. Нашёл reviewer при поправке пунктов 4 и 9.

### Найдено разбором жалоб владельца (всё — давнее, не регресс партии)

- **Кнопки углов задают только рыскание, но не наклон.** «Сзади» — это вид сзади-**сверху** под 26.565° (`src/main.js`, `ANGLE_CHIPS`: у «Сзади» только `yaw: 180`; `camera.pitch` не трогается, по умолчанию `PITCH_ISO_2_1`). Поэтому в нём законно видно зеркала и кромку кабины сбоку от кузова — владелец принял это за лишнюю геометрию. Замер: лишних вокселей **0** по всем шести видам; в ортографическом виде (pitch 0) модель ложится на рисунок без единого лишнего пикселя.
- **`guessViews` на листе из шести ячеек кладёт все шесть рисунков не в свои слоты.** Запасной порядок чтения — `front, right, top, back, left, bottom` (`src/core/sheet.js`), а грузовик разложен `front, back, right, left, top, bottom`. Примешь подсказку диалога как есть — бока становятся «верхом» и «задом», модель 3 360 885 вокселей вместо 3 174 409. Нужно распознавание шести ячеек по размерам (пары одинаковых), как уже сделано для трёх.
- **Автоподбор сетки упирается в 256** (`suggestGridSize`, `Math.min(256, need)`), хотя арту нужно 505, а в списке есть 384 и 512; и в селекте по умолчанию стоит 32³. Из-за этого скругление кабины (5 px на кабине 246 px) выходит квадратом: ступени появляются с 160, читаются с 192–256 (3 из 5), полностью — только на 512. Само по себе скругление — **предел сетки, не дефект** (залит 1 пиксель, съедено 12; числа на `dece678` и сейчас совпадают на всех десяти сетках).
- **Просьба владельца (новая функция):** порог слияния — ползунком, чтобы выбирал художник, с живым счётом «освободится N слотов»; по умолчанию остаётся 1.0. План считается за 6.9 мс на полной палитре. **Владелец решил: в очередь** (2026-09-24), не в текущую партию.

