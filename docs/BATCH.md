# Текущая партия

Файл ведёт чат разработки: `/start` записывает сюда снятый список, `/done`
очищает. Чат замысла его не правит — так два чата в одной папке не пишут в
один файл одновременно (`CLAUDE.md`, «Два чата»).

Снята `/start all` 2026-09-23 с «Очереди» `docs/ROADMAP.md` на `dece678`.
Данные пользователя: не задевает (`docs/TESTING.md`, «Данные пользователя» —
продукт хранит вне вкладки только тему).
Исходная проверка: полная, зелёная на `dece678` — `check.mjs` 30/30 модулей,
`mcp-smoke.mjs` все проверки. Состояние полигона — «строится», поэтому до
пункта 2 одобрение `reviewer` значит только «запускается без ошибок»
(`docs/TESTING.md`, «Что проверяется уже сейчас»).

**Команда полигона после пункта 2:** `node tests/run.mjs` — всё,
`node tests/run.mjs <имя>` — одна проверка. Записано здесь, а не в
`docs/TESTING.md`, потому что во время партии документы, кроме этого файла и
спецификаций, не правятся; текст для `docs/TESTING.md` лежит в графе «В
документы при `/done`» пункта 2.

| № | Пункт очереди | Критерий готовности | Состояние | В документы при `/done` | Свои числа |
|---|---|---|---|---|---|
| 1 | **[баг]** Пересборка молча стирает ручные правки (очередь 1) | Ни одно из десяти действий не теряет ручные правки без ведома пользователя; Ctrl+Z ведёт себя предсказуемо. Точный критерий — в `docs/BUGS.md`, строка «Готово, когда» | готов к проверке | TESTING — ключи i18n 177 → 179 (порог для i18n/parity); TESTING — приём замера для UI без полигона: подменить window.confirm из консоли и гнать main.js настоящими DOM-событиями; BUGS — удалить запись «Пересборка молча стирает все ручные правки»; CONCEPT — пересборка спрашивает, когда есть что потерять | Порогов нет. rebuild-guard 8/8, красный откатом History.hasEdits (7 из 8 падают). i18n 179=179. **Ограничение reviewer:** полигон «строится», одобрение значит «запускается без ошибок»; диалог и 10 UI-путей сверены чтением кода, пути loadInto и applySheet браузером не проверены — нужен файл владельца |
| 2 | **[код]** Построить проверки продукта (очередь 2) | `node tests/run.mjs` зелёный; каждая проверка из списка `ROADMAP.md` §1 проверена на красноту откатом соответствующего исправления. Текст для `docs/TESTING.md` **не писать в файл** — сдать его в эту таблицу, графу «В документы при `/done`» | готов к проверке | **TESTING — полигон теперь «есть».** Команды: проверка пункта `node tests/run.mjs <имя>`, группа `node tests/run.mjs <группа>/`, полная `node tools/check.mjs && node tools/mcp-smoke.mjs && node tests/run.mjs`, долгая `node tests/run.mjs perf/` (~13 с). **Без аргумента `run.mjs` пропускает группу `perf`** и пишет «1 long check(s) not run» — в TESTING описать именно так, а не «всё». TESTING — таблица проверок: `carve/cube-faces` 0/2400, `carve/reprojection` 0/10112 и 0 дыр, `carve/slope-banding` 84/1408 (потолок), `i18n/parity` 179=179, `perf/carve` 1.1/3.4/8.6 с и 107/142/249 МБ, `edit/rebuild-guard` 8/8. Ещё нет: `carve/visibility`, `views/extent-skew`, `views/spike`, `palette/accuracy`. TESTING — раздел «Как добавить проверку»: путь `tests/<группа>/<имя>.mjs`, `run()` → `{ok, detail}` (можно async), вход рисует `tests/fixtures.mjs` (`makeFlat`/`makeWedge`/`makeStep`/`makeFin`, `viewsOfShape` по предикату), длинные группы — в `LONG` внутри `run.mjs`. **BATCH №7 — переписать критерий:** «меньше 104 из 1914» — про грузовик владельца, полигон чужой арт не берёт; синтетический эквивалент — **84 из 1408**. Там же в TESTING, «Красное, которое бывает правильным»: slope-banding теперь зелёный с потолком 84, а не красный. CONCEPT — у продукта есть полигон поведения на синтетических видах; WebGL и вёрстка по-прежнему только глазами | 5 зелёных / 0 провалов. Потолок slope-banding 84/1408; сетки 20/32; шесть разных цветов видов (на одинаковых перекраска чужим видом невидима). perf baseline 1.13/3.38/8.64 с, 87/149/243 МБ, запас ×3. Краснота воспроизведена проверяющим в копии репозитория: cube-faces 232/2400, reprojection 634/10112 и 8/10112, slope-banding 1408/1408, parity 180 vs 179. **Ограничение reviewer:** формально «запускается без ошибок», т.к. TESTING правит `/done`; сверх того все четыре отката воспроизведены |
| 3 | **[баг]** Мазок рвётся при быстром движении (очередь 3) | Разрыв замерен, а не оценён на глаз; интерполяция между событиями указателя закрывает пунктир. Гипотеза о зеркале по X проверена тем же замером: подтвердилась — это отдельная запись в «Найдено по ходу». Критерий — в `docs/BUGS.md` | готов к проверке | TESTING — в таблицу проверок: `edit/stroke-gap` (порог 0 пропущенных; замер 0/32002 против 31309/32002 по лучу на событие), `edit/mirror-parity` (16/16), `edit/fill-drag` (бюджет 1000 мс, замер ~58 мс, 1501 выборка → 168 применений → 2 заливки); TESTING — «быстрая» теперь 31 модуль, не 30 (та же цифра в `CLAUDE.md`, «Запуск и проверка»); TESTING, «Чего проверки не видят» — у канваса `preserveDrawingBuffer: false`, поэтому `readPixels` задним числом даёт чёрное: мерить надо DOM-событиями и строкой состояния, картинку — снимком; BUGS — удалить запись «Мазок рвётся при быстром движении» (в ней уже устарела ссылка на `src/main.js:833`); CONCEPT — протяжка инструментом непрерывна, шаг 1/16 вокселя, один мазок остаётся одной записью истории | Шаг выборки 1/16 вокселя (1/8 оставлял до 4 граней), потолок 4096 выборок на отрезок, порог «грань пройдена» — ≥ 0.1 ширины вокселя. `BUDGET_MS = 1000` в fill-drag (×16 к замеренным ~60 мс). mirror-parity: сетки 20/21/32/33 × ширины 8/9/10/11. Краснота откатами: шаг в бесконечность → 31309/32002; `mirrorX` → `vol.nx - hit.x` → 0/16; снятый ранний возврат в `fillSurface` → 12766 мс. **Гипотеза о зеркале по X не подтвердилась:** `mirrorX` и `autoPlace` о центре согласны, 16/16. **Ограничение reviewer:** `continueStroke`/`getCoalescedEvents` браузером не проверены — в Node нет DOM |
| 4 | **[код]** Квантовать палитру при импорте (очередь 4) | Точность цвета на 256 выросла заметно против 47.5%; проверка `palette/accuracy` красная на старом алгоритме. Полный критерий — `ROADMAP.md`, «Подробности», §2 | готов к проверке | TESTING — таблица проверок: `palette/accuracy` (82.4% точно, 100% в пределах ΔE 2, 0/24576 видимо неверных, 255 из 324 цветов; краснота откатом: 816/24576, худшая ΔE 23.5) и `palette/delta-e` (18/18 пар Sharma, 4.9e-5; краснота — снять коррекцию 360° в среднем тоне); TESTING — быстрая теперь 33 модуля (та же цифра в `CLAUDE.md`); TESTING, «Замеры» — **точность цвета мерить ΔE2000, а не совпадением байтов**; **DECISIONS — обязательно:** происхождение CIEDE2000 — перепечатан руками из Sharma/Wu/Dalal 2005, сверен с MIT `image-q`, кода не импортировано (правило о чужом коде); CONCEPT — палитра выбирается квантизацией по всем шести видам, точные цвета крупных заливок сохраняются; **ROADMAP — критерий пункта переписать с «47.5%» на ΔE** — но только после решения владельца (см. «Свои числа») | Пороги: «видимо неверно» ΔE>10, «неразличимо» ΔE≤2 (JND ≈ 2.3); `RESERVE_SHARE` 0.002, `RESERVE_CAP` ¼ палитры; фикстура makeLivery 324 цвета. Квантизация 45.7 тыс. цветов — 135 мс; perf 1.1/3.5/9.0 с, 75/156/247 МБ, запас ×3 цел. **ВОПРОС ВЛАДЕЛЬЦУ (настройка, не блокирует приёмку):** на грузовике побайтовое совпадение упало (reviewer: 50.0% → 40.1%), зато ΔE>10 7.7% → 3.2%, средняя ΔE 3.05 → 2.17. Исполнитель утверждал «старую меру не поднять» — **reviewer это опроверг:** при `RESERVE_SHARE`=0.0005, `RESERVE_CAP`=0.6 выходит 47.2% побайтово (почти старые 50%) и при этом ΔE>10 3.4% вместо 7.7% — ценой доли «неотличимо» (87.8% → 80.5%). То есть это не «либо-либо», а ручка. **Ограничение reviewer:** формально «запускается без ошибок»; сверх того краснота обеих проверок и замер по грузовику воспроизведены самостоятельно |
| 5 | **[код]** Замена цвета во всей модели одним действием (очередь 5) | Смена цвета видна мгновенно на любом размере, Ctrl+Z её отменяет, сохранённый и снова загруженный проект показывает новый цвет. **Идентичность индекса палитры обязана пережить операцию.** `ROADMAP.md` §3 **От пункта 4:** кэш `_labs` в `src/core/palette.js` ключуется только длиной палитры — замена цвета на месте обязана его сбрасывать. | готов к проверке | TESTING — таблица проверок: `palette/recolour` (24 случая: индекс переживает замену, байты граней целы, старый ключ снят, дубль остаётся за своим слотом, кэш Lab сброшен, round-trip через `serialize`), `edit/palette-undo` (21 случай: драг = одна запись, `undo` возвращает `voxels`/`palette`, переплетение со штрихом, отказ без палитры); TESTING — ключи i18n 179 → 181, быстрая по-прежнему 33 модуля; TESTING, «Приём замера для UI» — пикер гнать настоящим `dblclick` по свотчу (для `showPicker()` нужен жест), затем `input`/`change` на `#edit-color`; синтетический `keydown` — на `document.body`, не на `window`; CONCEPT — двойной клик по свотчу перекрашивает модель целиком: индекс сохраняется, объём и геометрия не трогаются, цена — загрузка текстуры 256×1 на любом размере; DECISIONS — история стала типизированной (`{kind: voxels|palette}`), `undo`/`redo` возвращают вид шага, чтобы перекраска не тянула пересборку буфера граней | Порогов владелец не называл. Драг в проверке — 40 событий `input`, в браузере 1.0–1.3 мс на всю протяжку, одна запись истории. Краснота воспроизведена проверяющим в копии, включая главное: **подмена `replace()` на перенумерующий вариант → 13 провалов** — идентичность индекса действительно стережётся, а не декларируется; снятый сброс `_labs` → labsFresh=2 want 1. **Ограничение reviewer:** браузером не проверено — двойной клик, `showPicker()` и загрузка текстуры в WebGL в Node непроверяемы (исполнитель гонял их в браузере сам). **Глянуть глазами при приёмке:** отмена пикера в Chrome возвращает исходный цвет через `input` — в истории может остаться один «пустой» Ctrl+Z |
| 6 | **[ui]** Левая панель: редактор и палитра одним заходом (очередь 6) | Сначала `designer` пишет одну спецификацию на обе беды — превью действия инструмента и скроллбокс палитры. Затем исполнитель по ней. `ROADMAP.md` §5 и текст пункта 6 очереди | готов к проверке | Спецификация: `docs/specs/2026-09-24-6-left-panel.md`. TESTING — новые проверки `edit/preview`, `edit/usage`, `edit/fill-region`, `palette/order`; ключи i18n 181 → 204; быстрая теперь 35 модулей (та же цифра в `CLAUDE.md`); TESTING, «Приём замера для UI» — синтетические `PointerEvent` по канвасу + чтение `renderer.previewVertexCount` и строки состояния; **DESIGN.md §8 — строку `.palette-strip` заменить на `.palette-box` и `.palette-foot`** (компонент переделан, старый класс удалён); **DESIGN.md — шесть правил от `designer`:** признак прокручиваемости — обрезанный ряд без теней; бюджеты отзывчивости (превью 150 мс/20000 граней, счёт 100 мс иначе «—»); одна причина выключенного на группу; строка состояния при наведении возвращает прежнее сообщение; счётчик в подсказке, не цифрой на цели 20×20; группы 8+ целей — один шаг табуляции со стрелками; CONCEPT — инструмент показывает контуром, что тронет, и отчитывается числом за мазок; палитра — скроллбокс с полосами тона | Превью заливки 150 мс покоя / 20000 граней; счёт использования 300 мс задержки / 100 мс бюджета; порог нейтральности 0.12; 12 полос по 30°. Краснота воспроизведена reviewer: preview 24 вместо 12, usage 48 вместо 24, order 1 полоса вместо 3, fill-region молчит о пределе. Три отступления от спецификации reviewer признал обоснованными (числа 8/9 в коде нет вообще — перенос делает `flex-wrap`; в спецификации ошибка в ширине скроллбара). **ОГРАНИЧЕНИЕ — ГЛЯНУТЬ ГЛАЗАМИ ПРИ ПРИЁМКЕ:** раскладка на 1440+ и **палитра на 255 цветах (скролл, обрезанный ряд, полосы тона) снимками НЕ проверены** — у демо всего 5 цветов. Подтверждено структурно: `max-height:192` − рамка 2 − паддинг 12 = 178 при ряде 24 → 7.42 ряда, восьмой обрезан + `overflow-y:auto`. Снимки во встроенной панели: обе темы, счёт «граней 606», контур превью цветом акцента |
| 7 | **[код]** Photo hull: расхождение цвета режет геометрию (очередь 7) | Синяя полоска от стекла исчезла, `carve/slope-banding` меньше 104, ни одна существующая проверка не покраснела. `ROADMAP.md` §4 | **ждёт: решение владельца — см. вопрос ниже** | BUGS («Синяя полоска…», «Остаточная чересполосица…») — дописать: правило «резать по расхождению цвета» **опровергнуто замером**, числа в графе справа; **ROADMAP, «Photo hull» — куски 1, 3 и 4 в этом продукте не работают** (у вокселя шесть независимо покрашенных граней, так что «виды расходятся» — норма, а не дефект); критерий переписать на замер по грузовику, а не на `slope-banding`; TESTING, «Замеры» — воспроизводимая сборка грузовика из Node (detectCells+cropCell+fitViews, слоты front,back,right,left,top,bottom, 3 174 409 вокселей на 256); BLOCKED — вопрос 4 стал блокером этого пункта | **Кода нет — прототип откачен, дерево чистое, 16 зелёных / 0 как до пункта.** Замеры на `tests/art/truck.png` (3 174 409 вокселей — сборка та же, что у владельца): (1) **буквальное правило пункта режет фикстуры**: у голого куба 224 вокселя из 8000, у плавника все 1024 (при ΔE>40 ещё 94); (2) вариант на уровне грани **синтетический критерий выполняет** (slope-banding 84/1408 → 6/1314), но на грузовике ухудшает всё: срезано 5333 вокселя, не в цвет 7578 → 8975, противоречий 5397 → 6422, карвинг 1.28 → 2.11 с; за 6 проходов не сходится (срезано 40 522); (3) альтернатива без резки: не в цвет 7578 → 2181, но чересполосица 15/1639 → 1470/1639. **Критерий пункта стережёт не то:** он зеленеет от правки, которая портит настоящую модель. Побочно: чересполосица на грузовике сегодня 15/1639, а не 104/1914 — старое число нынешним определением угла не воспроизводится; и 6536 пикселей силуэта не попадают ни в один воксель уже сейчас, без photo hull |
| 8 | **[код]** Убрать сборку из главного потока (очередь 8) | На 512 вкладка отвечает во время сборки и виден прогресс. `ROADMAP.md` §6 | готов к проверке (3 круга) | TESTING — пять новых проверок: `carve/worker-parity` (32768 вокселей, 324 записи lookup, identical), `carve/volume-transfer` (91440 вокселей, 548640 байт граней, 0 различий), `carve/progress` (45 сообщений, худший шаг 10%, молчание 12%), `carve/worker-fallback` (3 случая), `edit/load-during-build`; TESTING — ключи i18n 204 → 215, быстрая **38** модулей (та же цифра в `CLAUDE.md`), полигон 21 проверка; TESTING, «Чего проверки не видят» — отзывчивость вкладки только в браузере (rAF-счётчик разрывов + `#build-progress-fill`); у `edit/load-during-build` половина структурная — `src/main.js` в Node не импортируется; **DESIGN §6, «Долгая работа» — устарел** (там написано «отклика вкладки нет»); **DESIGN §2, «Правило акцента» — узаконить `.progress-fill` с `--accent`** (как уже сделано с контуром превью); DESIGN §8 — новый компонент `.progress`/`.progress-fill`; CONCEPT — сборка идёт в воркере, вкладка отвечает, видна полоса и стадия; оба отказа воркера (`file://` и без модульных воркеров) кончаются карвингом на главном потоке; загруженный проект старше идущей сборки; DECISIONS — воркер, а не уступки главному потоку: одна реализация карвинга, байты граней переносятся дословно (`Volume.pack/unpack`); ROADMAP — пункт закрыт | **На 512: сборка 16.1 с, худший разрыв кадра главного потока 9 мс против 1992 мс без воркера** (44.2 млн вокселей, 1878 кадров, 65 шагов полосы, 7 стадий названы). perf без регресса: 1.1/3.7/9.0 с, 78/154/246 МБ. Веса стадий 0.05/0.25/0.12/0.17/0.12/0.29, троттлинг 1%/60 мс, пороги progress шаг ≤12.5% / молчание ≤20%. **Два круга замечаний, оба существенные:** (1) браузер без модульных воркеров вместо замерзшей вкладки получал отказ совсем — исправлено; (2) **асинхронная сборка снова открыла потерю данных, закрытую пунктом 1** (загрузка проекта поверх идущей сборки) — исправлено `dropBuild()`. **ОГРАНИЧЕНИЕ — ГЛЯНУТЬ ГЛАЗАМИ:** «на 512 вкладка отвечает» и вид полосы в Node непроверяемы — числа выше сняты исполнителем в предпросмотре, reviewer их не повторял |
| 9 | **[код]** Слияние почти-дубликатов палитры и освобождение слотов (очередь 11) | Переполнение палитры перестаёт быть тупиком: близкие цвета сливаются, слоты освобождаются, строка состояния говорит правду | готов к проверке (3 круга) | TESTING — три новые проверки: `palette/merge` (45 случаев), `edit/merge-undo` (31), `edit/merge-offer` (47: 7 состояний кнопки замером + 6 чтений исходника, включая устаревание плана после перекраски); TESTING — ключи i18n 215 → 225, быстрая **39** модулей (та же цифра в `CLAUDE.md`), полигон 24 проверки; TESTING, «Замеры» — стоимость слияния: план 6.9 мс на 255 цветах, `remapFaces` 18 мс на 256 / 41 мс на 384, запись отмены 9 байт на грань (1.6 МБ при 189 000 граней); CONCEPT — переполнение палитры больше не тупик: почти-дубликаты сливаются, слот освобождается (не перенумеровывается), отменяется одним Ctrl+Z; слияние требует модели и не спорит со сборкой, причина всегда написана под кнопкой; DECISIONS — порог ΔE2000 1.0 (половина JND), «keeper — тот, кто красит больше», группировка keeper-first вместо single linkage (иначе градиент схлопывается целиком), дыры хранятся в проекте (`serialize.free`); **DECISIONS — палитра для внешних потребителей (`sprites.json` в браузере и в MCP) перечисляется через `Palette.slots()`, а не позиционным срезом**; DESIGN §8 — кнопка `.ghost.small` «Слить дубликаты» и строка-причина `#merge-note` (новых классов нет; причина выключенного — `.hint.warn`); ROADMAP — пункт закрыт | `MERGE_DELTA_E` = 1.0 — по прогону на грузовике: освобождает 65 слотов при 0.5, **136 при 1.0**, 174 при 2.0, 193 при 3.0 из 255. Три круга: (1) кнопка была активна без модели и во время сборки, нажатие молча ничего не делало; (2) причина выключенного шла не `.hint.warn`; (3) **план слияния устаревал после перекраски (пункт 5) и перекрашивал грани в чужой цвет** — всё исправлено и застережено проверками. Краснота воспроизведена reviewer, включая главное: подмена дыр на компактирование (`colors.splice`) → 9 провалов — правило «не перенумеровывать» действительно стережётся. **Знать владельцу:** `refreshMergeOffer()` считает план заново — на полной палитре 16.2 мс, а он висит на `input` пикера: на 255 цветах протяжка пикера может рваться (бюджет DESIGN 100 мс цел, критерий не нарушен). **Ограничение reviewer:** кнопка, подпись и перезалив текстуры в Node непроверяемы |

Состояния: ждёт очереди · в работе · готов к проверке · ждёт: <вопрос> ·
не выполнен: <замечания>

Номера в этой таблице — свои, сквозные; в скобках указан номер пункта в
«Очереди» `docs/ROADMAP.md` на момент снимка. `/done кроме 3` считает по
левой колонке.

**Не взято в партию:** пункты 9 и 10 «Очереди» (ложное срабатывание
«одинаковых видов» на демо и синяя полоска от стекла) — у них метка маршрута
`[баг]`, но нет метки состояния `[можно]`, а `/start all` снимает пункты с
`[можно]`. Пункт 12 очереди — `[ждёт: решение владельца]`. Подробнее — в
отчёте владельцу.

## Пришло — внести в очередь

Сюда пишет `/add`; переносит в `ROADMAP.md` и `BLOCKED.md` чат замысла.

## Найдено по ходу — внести в очередь

Сюда пишет координатор `/start`: что исполнители заметили вне своего пункта.

- **Рисунок с чётностью ширины, противоположной сетке, теряет целый столбец модели.** Замер `tests/edit/mirror-parity.mjs` (пункт 3): 8 случаев из 16 — нарисовано 9 px, вырезано 8 (сетка 32); нарисовано 8, вырезано 7 (сетка 33). Причина: `autoPlace` округляет половину зазора одинаково для всех видов (`src/core/views.js`), но у вида `back` ось u идёт обратно (`N - 1 - u`, `src/core/views.js:34`), поэтому перёд и зад садятся на клетку врозь и их пересечение уже любого из них. Дефект карвинга, не редактора.
- **При расхождении переднего и заднего рисунков по ширине на нечётное число пикселей модель стоит на полвокселя мимо оси сетки**, и зеркальная правка ложится соседом (замерено: смещение −1 при 9 и 12 px). Тот же замер. Тоже карвинг.
- Исходная гипотеза пункта 3 («`autoPlace` и `mirrorX` считают центр по-разному») **замером опровергнута** — 16/16, снос 0. В очередь ставить нечего, но в `BUGS.md` этого предположения больше быть не должно.
- **Сохранённый и снова загруженный проект теряет таблицу соответствия цветов** (`serialize` хранит только сами цвета). На модель не влияет, влияет на добор цвета пипеткой после загрузки. Замечено в пункте 4.
- **`src/main.js`, обработчик `keydown` делает `e.target.matches(...)` без проверки, что цель — элемент.** При событии на `window`/`document` бросает `TypeError`. Настоящей клавиатурой недостижимо, но ломает любой синтетический замер горячих клавиш — то есть мешает полигону. Замечено в пункте 5.
- **«Очистить» при пустых слотах не сбрасывает палитру**: `build()` выходит раньше, и в палитре остаются цвета снесённой модели. Замечено в пункте 6.
- **`src/gfx/renderer.js` — цвет контура превью был зашит числами** (акцент тёмной темы), в светлой неверный — нарушение `DESIGN.md` §2. Нашёл `designer`, исправлено в пункте 6 (читается из `--accent`). Запись здесь — чтобы не потерять правило.
- **Обработчик `drop` на `document` зовёт `e.target.closest`** — при событии на сам `document` бросает `TypeError`. Та же болезнь, что у `keydown` из пункта 5. Замечено в пункте 8.
- **Кнопки «Загрузить проект», «Демо» и загрузка вида во время сборки не выключены.** Данные больше не теряются (пункт 8), но панель молчит о том, что сборка снята. Замечено в пункте 8.
- **`undo()`/`redo()` во время сборки говорят `status.nothingToUndo`** — формально неправда: история есть, просто занята. `DESIGN.md` требует объяснять отказ. Замечено в пункте 8.
- **`mcp-smoke.mjs` не смотрит содержимое `sprites.json` дальше размеров листа** — правку палитры в MCP он бы не поймал. Полигонной проверки на экспорт MCP нет. Замечено в пункте 9.
- **`css/app.css` — `.hint.warn { color: var(--warn, #d08a3a) }` держит зашитый запасной цвет** вопреки `DESIGN.md` §2 (цвет через переменную). Замечено в пункте 9.
- **После слияния повторное добавление цвета из арта возвращает прежний слот-псевдоним**, а не свободный: псевдонимы квантизации намеренно переживают слияние, свободные слоты достаются только новым цветам пользователя. Замечено в пункте 9.

