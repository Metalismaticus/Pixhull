# Текущая партия

Файл ведёт чат разработки: `/start` записывает сюда снятый список, `/done`
очищает. Чат замысла его не правит — так два чата в одной папке не пишут в
один файл одновременно (`CLAUDE.md`, «Два чата»).

Снята `/start all` 2026-09-23 с «Очереди» `docs/ROADMAP.md` на `dece678`.
Данные пользователя: не задевает (`docs/TESTING.md`, «Данные пользователя» —
продукт хранит вне вкладки только тему).
Исходная проверка: полная, зелёная на `dece678` — `check.mjs` 30/30 модулей,
`mcp-smoke.mjs` все проверки. Состояние полигона — «строится», поэтому до
пункта 2 одобрение `reviewer` значит только «запускается без ошибок»
(`docs/TESTING.md`, «Что проверяется уже сейчас»).

**Команда полигона после пункта 2:** `node tests/run.mjs` — всё,
`node tests/run.mjs <имя>` — одна проверка. Записано здесь, а не в
`docs/TESTING.md`, потому что во время партии документы, кроме этого файла и
спецификаций, не правятся; текст для `docs/TESTING.md` лежит в графе «В
документы при `/done`» пункта 2.

| № | Пункт очереди | Критерий готовности | Состояние | В документы при `/done` | Свои числа |
|---|---|---|---|---|---|
| 1 | **[баг]** Пересборка молча стирает ручные правки (очередь 1) | Ни одно из десяти действий не теряет ручные правки без ведома пользователя; Ctrl+Z ведёт себя предсказуемо. Точный критерий — в `docs/BUGS.md`, строка «Готово, когда» | готов к проверке | TESTING — ключи i18n 177 → 179 (порог для i18n/parity); TESTING — приём замера для UI без полигона: подменить window.confirm из консоли и гнать main.js настоящими DOM-событиями; BUGS — удалить запись «Пересборка молча стирает все ручные правки»; CONCEPT — пересборка спрашивает, когда есть что потерять | Порогов нет. rebuild-guard 8/8, красный откатом History.hasEdits (7 из 8 падают). i18n 179=179. **Ограничение reviewer:** полигон «строится», одобрение значит «запускается без ошибок»; диалог и 10 UI-путей сверены чтением кода, пути loadInto и applySheet браузером не проверены — нужен файл владельца |
| 2 | **[код]** Построить проверки продукта (очередь 2) | `node tests/run.mjs` зелёный; каждая проверка из списка `ROADMAP.md` §1 проверена на красноту откатом соответствующего исправления. Текст для `docs/TESTING.md` **не писать в файл** — сдать его в эту таблицу, графу «В документы при `/done`» | ждёт очереди | | |
| 3 | **[баг]** Мазок рвётся при быстром движении (очередь 3) | Разрыв замерен, а не оценён на глаз; интерполяция между событиями указателя закрывает пунктир. Гипотеза о зеркале по X проверена тем же замером: подтвердилась — это отдельная запись в «Найдено по ходу». Критерий — в `docs/BUGS.md` | ждёт очереди | | |
| 4 | **[код]** Квантовать палитру при импорте (очередь 4) | Точность цвета на 256 выросла заметно против 47.5%; проверка `palette/accuracy` красная на старом алгоритме. Полный критерий — `ROADMAP.md`, «Подробности», §2 | ждёт очереди | | |
| 5 | **[код]** Замена цвета во всей модели одним действием (очередь 5) | Смена цвета видна мгновенно на любом размере, Ctrl+Z её отменяет, сохранённый и снова загруженный проект показывает новый цвет. **Идентичность индекса палитры обязана пережить операцию.** `ROADMAP.md` §3 | ждёт очереди | | |
| 6 | **[ui]** Левая панель: редактор и палитра одним заходом (очередь 6) | Сначала `designer` пишет одну спецификацию на обе беды — превью действия инструмента и скроллбокс палитры. Затем исполнитель по ней. `ROADMAP.md` §5 и текст пункта 6 очереди | ждёт очереди | | |
| 7 | **[код]** Photo hull: расхождение цвета режет геометрию (очередь 7) | Синяя полоска от стекла исчезла, `carve/slope-banding` меньше 104, ни одна существующая проверка не покраснела. `ROADMAP.md` §4 | ждёт очереди | | |
| 8 | **[код]** Убрать сборку из главного потока (очередь 8) | На 512 вкладка отвечает во время сборки и виден прогресс. `ROADMAP.md` §6 | ждёт очереди | | |
| 9 | **[код]** Слияние почти-дубликатов палитры и освобождение слотов (очередь 11) | Переполнение палитры перестаёт быть тупиком: близкие цвета сливаются, слоты освобождаются, строка состояния говорит правду | ждёт очереди | | |

Состояния: ждёт очереди · в работе · готов к проверке · ждёт: <вопрос> ·
не выполнен: <замечания>

Номера в этой таблице — свои, сквозные; в скобках указан номер пункта в
«Очереди» `docs/ROADMAP.md` на момент снимка. `/done кроме 3` считает по
левой колонке.

**Не взято в партию:** пункты 9 и 10 «Очереди» (ложное срабатывание
«одинаковых видов» на демо и синяя полоска от стекла) — у них метка маршрута
`[баг]`, но нет метки состояния `[можно]`, а `/start all` снимает пункты с
`[можно]`. Пункт 12 очереди — `[ждёт: решение владельца]`. Подробнее — в
отчёте владельцу.

## Пришло — внести в очередь

Сюда пишет `/add`; переносит в `ROADMAP.md` и `BLOCKED.md` чат замысла.

## Найдено по ходу — внести в очередь

Сюда пишет координатор `/start`: что исполнители заметили вне своего пункта.
